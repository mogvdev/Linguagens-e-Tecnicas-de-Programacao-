#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	
	double valor;
	printf("Primeiro valor: ");
	scanf ("%lf", &valor);
	
	printf("Valor em notacao: %e", valor);
	
	
	
	return 0;
}
