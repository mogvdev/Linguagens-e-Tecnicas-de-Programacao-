#include <stdio.h>
int main()
{
    int a1;
    printf("Digite um numero: ");
    scanf("%d", &a1);
    
    if (a1 %2 == 0){ 
        printf("Seu numero é par!");
    }
    else{
        printf("Seu numero é impar!");
    }
    
    
}
